# ARM64 (Snapdragon X Elite) + amd64
FROM --platform=$BUILDPLATFORM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi>=0.111.0 \
    uvicorn[standard]>=0.29.0

COPY main.py .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
